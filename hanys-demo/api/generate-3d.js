export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{const key=process.env.NVIDIA_API_KEY;if(!key)return res.status(500).json({error:'Brak NVIDIA_API_KEY w Vercel Environment Variables.'});const {image,prompt='cute detailed Polish bebok character, full body, front-facing, preserve the reference character design, stylized high-quality game-ready 3D model'}=req.body||{};if(!image||typeof image!=='string')return res.status(400).json({error:'image is required as a data URL'});const r=await fetch('https://ai.api.nvidia.com/v1/genai/microsoft/trellis',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({mode:'image',image,prompt:prompt.slice(0,77),output_format:'glb',samples:1,seed:0,no_texture:false,slat_cfg_scale:7.5,ss_cfg_scale:7.5,slat_sampling_steps:50,ss_sampling_steps:50})});const text=await r.text();if(!r.ok)return res.status(r.status).send(text);let data;try{data=JSON.parse(text)}catch{data={raw:text}}return res.status(200).json(data)}catch(e){return res.status(500).json({error:e?.message||'NVIDIA request failed'})}}