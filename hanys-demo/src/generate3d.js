export async function generateHanys3D(file){const dataUrl=await fileToDataUrl(file);const r=await fetch('/api/generate-3d',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:dataUrl,prompt:'cute detailed Polish bebok character, full body, preserve the exact reference design, stylized premium game-ready 3D character, clean silhouette, detailed clothing and face'})});const data=await r.json();if(!r.ok)throw new Error(data.error||'3D generation failed');const b64=data?.artifacts?.[0]?.base64||data?.artifact?.base64;if(!b64)throw new Error('NVIDIA returned no GLB artifact');return Uint8Array.from(atob(b64),c=>c.charCodeAt(0));}
function fileToDataUrl(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)})}